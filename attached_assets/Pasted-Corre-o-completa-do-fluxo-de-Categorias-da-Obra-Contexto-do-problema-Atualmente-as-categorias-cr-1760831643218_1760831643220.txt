Correção completa do fluxo de Categorias da Obra

Contexto do problema:
Atualmente, as categorias criadas em uma obra são salvas corretamente no banco de dados e aparecem na página de visualização da obra.
No entanto, ao acessar a tela de edição da obra (/projects/:id/edit), as categorias não são carregadas nem exibidas na interface, impedindo edição ou exclusão.
Além disso, essas categorias devem estar disponíveis e selecionáveis ao criar novos relatórios associados à obra.

🎯 Objetivo da correção

Garantir que:

As categorias criadas fiquem salvas corretamente no banco de dados com vínculo ao project_id.

Na tela de edição da obra, todas as categorias vinculadas ao projeto sejam carregadas e exibidas na lista de categorias.

Permitir adicionar, editar e excluir categorias diretamente na tela de edição.

As categorias também apareçam corretamente:

Na aba de visualização da obra (como já ocorre).

No formulário de criação de relatório, para seleção da categoria de fotos.

Após salvar alterações na obra, manter as categorias existentes e refletir corretamente no banco.

Se necessário, corrigir o método de carregamento das categorias na rota /projects/<id>/edit, incluindo ajustes em views.py e nos templates.

🛠️ Etapas sugeridas para o desenvolvedor
1️⃣ Back-end (Flask / Django / FastAPI)

Verificar se o modelo Category contém o campo project_id com ForeignKey(Project.id) corretamente configurado.

Na rota /projects/<id>/edit, garantir que o queryset de categorias seja carregado e enviado ao template:

project = Project.query.get(project_id)
categories = Category.query.filter_by(project_id=project.id).order_by(Category.order.asc()).all()
return render_template("edit_project.html", project=project, categories=categories)


Corrigir endpoints de criação, edição e exclusão de categorias para usar project_id corretamente:

@app.route('/categories/create', methods=['POST'])
def create_category():
    project_id = request.form.get('project_id')
    name = request.form.get('name')
    new_category = Category(name=name, project_id=project_id)
    db.session.add(new_category)
    db.session.commit()
    return redirect(url_for('edit_project', id=project_id))


Criar ou revisar endpoint DELETE para exclusão segura:

@app.route('/categories/<int:id>/delete', methods=['POST'])
def delete_category(id):
    category = Category.query.get(id)
    project_id = category.project_id
    db.session.delete(category)
    db.session.commit()
    return redirect(url_for('edit_project', id=project_id))

2️⃣ Front-end (Template / JavaScript)

Na página de edição da obra, garantir que o bloco de categorias seja populado com os dados passados do backend:

{% for category in categories %}
    <div class="category-item">
        <span>{{ category.name }}</span>
        <button class="btn-edit" data-id="{{ category.id }}">Editar</button>
        <button class="btn-delete" data-id="{{ category.id }}">Excluir</button>
    </div>
{% endfor %}


Adicionar script para manipular AJAX ou submissão de formulários sem recarregar a página (opcional, caso o fluxo use modal).

Garantir que ao clicar em Editar, os dados da categoria sejam carregados no campo do modal corretamente.

Após Salvar, atualizar a lista sem reload total da página (ou com reload direcionado).

3️⃣ Banco de Dados (Alembic)

Se o campo project_id não existir em Category, criar migração:

alembic revision --autogenerate -m "Add project_id to Category"
alembic upgrade head


No modelo:

class Category(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(255), nullable=False)
    order = db.Column(db.Integer, default=0)
    project_id = db.Column(db.Integer, db.ForeignKey('project.id'), nullable=False)

4️⃣ Integração com Relatórios

No formulário de criação de relatório, filtrar apenas categorias vinculadas ao projeto selecionado:

categories = Category.query.filter_by(project_id=selected_project.id).all()


Exibir essas categorias em um <select>:

<select name="category_id" required>
    {% for category in categories %}
        <option value="{{ category.id }}">{{ category.name }}</option>
    {% endfor %}
</select>

✅ Checklist de verificação final

 Ao criar uma categoria em editar obra, ela aparece imediatamente na lista.

 Ao entrar novamente em editar obra, todas as categorias cadastradas aparecem corretamente.

 É possível editar e excluir categorias.

 As categorias aparecem corretamente na aba de visualização da obra.

 As categorias aparecem no formulário de criação de relatório.

 Após salvar, tudo é persistido corretamente no banco.