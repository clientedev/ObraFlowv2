# This file keeps the uploads directory in version control
# The uploads directory is used for storing user-uploaded files like photos
# This .gitkeep file ensures the directory exists even when empty
